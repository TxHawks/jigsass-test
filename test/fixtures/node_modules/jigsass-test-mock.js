'use strict';

// We simulate jigsass-test being installed in node-modules/jigsass-test/lib by
// putting this file in test/node-modules/jigsass-test/ and then exporting
// jigsass-test.
var jigsassTest = require('../../../lib/jigsass-test.js'),
  path = require('path');

// Reset the default paths variable for this new file location.
jigsassTest.paths = {
  // This is a copy of the lines from lib/jigsass-test.js.
  fixtures: path.join(__dirname, '../../../', 'test/fixtures'),
  includePaths: [path.join(__dirname, '../../../', 'scss')]
};

module.exports = jigsassTest;
